import { Section } from '@/components/layout/Section'

export function LoginPage() {
  return (
    <Section
      kicker="Authentication"
      title="Login"
      description="Placeholder route for the future sign-in flow."
    />
  )
}